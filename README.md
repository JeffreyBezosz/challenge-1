# Challenge 1 - Express

Simple Node.js + Express application for the Express challenge.

## Run locally

```bash
npm install
npm start
```

Then open:

```
http://localhost:3000
```

The homepage displays **Jef Vereecke**.

## Render

- Build command: `npm install`
- Start command: `npm start`
